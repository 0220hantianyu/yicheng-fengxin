{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "test -d client && cd client; npm install --no-audit --no-fund --prefer-offline && npm run build",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
