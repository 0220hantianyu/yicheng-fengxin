# 一程风信 - 如何打开项目

## 方法一：双击启动脚本（最简单）

1. 打开文件资源管理器，进入 `D:\Projects\一程风信`
2. 双击 `启动项目.bat`
3. 会弹出两个命令行窗口（后端和前端），等待几秒
4. 浏览器会自动打开 http://localhost:5173

## 方法二：手动命令行启动

打开**两个**命令行窗口（CMD 或 PowerShell）：

**窗口 1 - 启动后端：**
```
cd D:\Projects\一程风信
node --import tsx server/src/index.ts
```
看到 `Server running on port 3001 (Mock mode)` 即成功。

**窗口 2 - 启动前端：**
```
cd D:\Projects\一程风信\client
npx vite --host --port 5173
```
看到 `Local: http://localhost:5173/` 即成功。

然后在浏览器打开 http://localhost:5173

## 方法三：一键启动（需要两个服务都先停掉）

```
cd D:\Projects\一程风信
npm run dev
```

## 注意事项

- 后端默认运行在 **3001** 端口，前端在 **5173** 端口
- 当前没有配置真实天气 API Key，所以是**演示模式**（界面会标注"演示数据"）
- 关闭命令行窗口即停止对应的服务
- 如果端口被占用，先关掉之前的命令行窗口再重新启动
